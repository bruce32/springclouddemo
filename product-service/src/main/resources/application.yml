server:
  port: 8301
spring:
  application:
    name: productservice
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
       defaultZone: http://localhost:8101/eureka